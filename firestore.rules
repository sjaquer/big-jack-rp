
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Regla global: si el usuario ha iniciado sesión, puede leer y escribir en cualquier documento.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
